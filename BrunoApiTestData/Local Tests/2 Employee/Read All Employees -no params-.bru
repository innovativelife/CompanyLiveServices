meta {
  name: Read All Employees -no params-
  type: http
  seq: 15
}

get {
  url: http://{{LOCALDEVURL}}/api/v1/tenants/tenant1/Employees
  body: json
  auth: bearer
}

headers {
  uId: tester
  tenantId: Tenant1
}

auth:bearer {
  token: {{AuthToken}}
}

tests {
  test("Status code is 200 (Not Found)", function () {
      expect(res.getStatus()).to.equal(200);
  });
  
  const jsonResponse = res.getBody();
  
  test("at least three employees returned", function () {
      expect(jsonResponse.employees.length>=3);
  });
  
}
