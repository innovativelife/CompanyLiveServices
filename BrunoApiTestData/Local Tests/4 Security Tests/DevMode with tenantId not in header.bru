meta {
  name: DevMode with tenantId not in header
  type: http
  seq: 1
}

get {
  url: http://{{LOCALDEVURL}}/Employees/tenant1
  body: none
  auth: bearer
}

headers {
  uId: aValue
}

auth:bearer {
  token: {{AuthToken}}
}

tests {
  test("Status code is 401 (Ok)", function () {
      expect(res.getStatus()).to.equal(401);
  });
}
