meta {
  name: Read All Employees - Cross Tenant
  type: http
  seq: 9
}

get {
  url: https://{{URL}}/api/v1/Tenants/tenant999/Employees
  body: none
  auth: bearer
}

auth:bearer {
  token: {{NewTenant-Admin1-AuthToken}}
}

tests {
  test("Status code is 401 (Unauthorized) for " + "{{Admin1-AuthToken}}", function () {
      expect(res.getStatus()).to.equal(401);
  });
  
  
}
