meta {
  name: Reset Password Error - Cross Tenant
  type: http
  seq: 5
}

put {
  url: http://{{URL}}/Employees/tenant999/{{Tenant999-AddedEmployeeUID}}/resetpassword/NewPassword
  body: json
  auth: bearer
}

auth:bearer {
  token: {{NewTenant-Admin1-AuthToken}}
}

body:json {
  {
  }
}

tests {
  test("Status code is 401 (Unauthorized)", function () {
      expect(res.getStatus()).to.equal(401);
  });
  
}
