meta {
  name: Set Admin Privilege Error - Cross Tenant
  type: http
  seq: 7
}

patch {
  url: https://{{URL}}/api/v1/Tenants/tenant999/Employees/{{Tenant999-AddedEmployeeUID}}/admin/true
  body: json
  auth: bearer
}

auth:bearer {
  token: {{NewTenant-Admin1-AuthToken}}
}

body:json {
  {
  }
}

tests {
  test("Status code is 401 (Unauthorized)", function () {
      expect(res.getStatus()).to.equal(401);
  });
  
}
