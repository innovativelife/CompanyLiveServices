meta {
  name: Reset Password Error - In Tenant
  type: http
  seq: 6
}

put {
  url: https://{{URL}}/Employees/tenant666/{{Tenant999-AddedEmployeeUID}}/resetpassword/NewPassword
  body: json
  auth: bearer
}

auth:bearer {
  token: {{NewTenant-Admin1-AuthToken}}
}

body:json {
  {
  }
}

tests {
  test("Status code is 404 (Not Found)", function () {
      expect(res.getStatus()).to.equal(404);
  });
  
}
