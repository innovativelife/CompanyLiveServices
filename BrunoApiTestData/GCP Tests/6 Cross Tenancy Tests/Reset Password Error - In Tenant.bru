meta {
  name: Reset Password Error - In Tenant
  type: http
  seq: 6
}

patch {
  url: https://{{URL}}/api/v1/Tenants/tenant666/Employees/{{Tenant999-AddedEmployeeUID}}/resetpassword/NewPassword
  body: json
  auth: bearer
}

auth:bearer {
  token: {{NewTenant-Admin1-AuthToken}}
}

body:json {
  {
  }
}

tests {
  test("Status code is 404 (Not Found)", function () {
      expect(res.getStatus()).to.equal(404);
  });
  
}
