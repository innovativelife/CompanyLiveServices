meta {
  name: Set Admin Privilege for Greg Innovate
  type: http
  seq: 13
}

patch {
  url: http://{{URL}}/api/v1/Tenants/tenant999/Employees/{{Tenant999-GregSmith}}/admin/true
  body: json
  auth: bearer
}

auth:bearer {
  token: {{Admin1-AuthToken}}
}

body:json {
  {
  }
}

tests {
  test("Status code is 200 (Ok) for " + "{{Admin1-AuthToken}}", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
