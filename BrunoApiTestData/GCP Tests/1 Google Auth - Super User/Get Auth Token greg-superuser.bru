meta {
  name: Get Auth Token greg-superuser
  type: http
  seq: 1
}

post {
  url: https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyPassword?key={{AUTHKEY}}
  body: json
  auth: none
}

params:query {
  key: {{AUTHKEY}}
}

body:json {
  {
      "email": "{{ADMINID}}",
      "password": "{{ADMINPWD}}",
      "tenantId": "{{ROOTTENANT}}",
      "returnSecureToken": true
  }
}

script:post-response {
  const jsonResponse = res.getBody();
  
  bru.setVar("AuthToken", jsonResponse.idToken)
}

tests {
  test("Status code is 200 (Ok)", function () {
      expect(res.getStatus()).to.equal(200);
  });
  
  const jsonResponse = res.getBody();
  
  test("ID Token returned", function () {
      expect(jsonResponse.idToken.length).to.greaterThan(0);
  });
  
}
