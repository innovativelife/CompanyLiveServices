meta {
  name: Reset Password  - Error for non Admin
  type: http
  seq: 5
}

patch {
  url: http://{{URL}}/api/v1/Tenants/tenant999/Employees//{{Tenant999-AddedEmployeeUID}}/resetpassword/NewPassword
  body: json
  auth: bearer
}

auth:bearer {
  token: {{NonAdmin-AuthToken}}
}

body:json {
  {
  }
}

tests {
  test("Status code is 403 (Forbidden)", function () {
      expect(res.getStatus()).to.equal(403);
  });
  
}
